#include<iostream>
#include<vector>
#include<algorithm>

using namespace std;
void f(int x[5]){
    for(int i=0;i<5;i++)
        x[i]*=11;
}
int main(){
    int arr[5]={1,4,6,7,8};
    int n=sizeof(arr)/sizeof(arr[0]);
    int z=sizeof(arr)/sizeof(arr[1]);
    fo(int f){
        
    }
    cout<<n<<endl;
    f(arr);
    for(int i=0;i<n;i++){
        cout<<arr[i]<<" ";
    }
    vector<int> A(5);
    fill(A.begin(),A.end(),0);
    for(int i=0;i<n;i++){
        cout<<A[i]<<" ";

    }
    find(A.)
    //Array are passed by REFERENCE default
}
